# TaskFence Demo Workspace

This fixture is used by `taskfence run examples/task.yaml`.

The demo task writes `src/taskfence-demo.txt` through the Docker runner and
records structured evidence under `.taskfence/tasks/local-demo/`.
